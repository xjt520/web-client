import { type Lifecycle } from '../lib/autogen/types';
import type { ParamsObj, Reducer } from '../lib/reducers';
import { type UntypedSchemaDef } from '../lib/schema';
import { RowBuilder, type RowObj } from '../lib/type_builders';
import { type ModuleExport, type SchemaInner } from './schema';
export interface ReducerExport<S extends UntypedSchemaDef, Params extends ParamsObj> extends Reducer<S, Params>, ModuleExport {
}
export interface ReducerOpts {
    name: string;
}
export declare function makeReducerExport<S extends UntypedSchemaDef, Params extends ParamsObj>(ctx: SchemaInner, opts: ReducerOpts | undefined, params: RowObj | RowBuilder<RowObj>, fn: Reducer<any, any>, lifecycle?: Lifecycle): ReducerExport<S, Params>;
/**
 * internal: pushReducer() helper used by reducer() and lifecycle wrappers
 *
 * @param name - The name of the reducer.
 * @param params - The parameters for the reducer.
 * @param fn - The reducer function.
 * @param lifecycle - Optional lifecycle hooks for the reducer.
 */
export declare function registerReducer(ctx: SchemaInner, exportName: string, params: RowObj | RowBuilder<RowObj>, fn: Reducer<any, any>, opts?: ReducerOpts, lifecycle?: Lifecycle): void;
export type Reducers = Reducer<any, any>[];
//# sourceMappingURL=reducers.d.ts.map